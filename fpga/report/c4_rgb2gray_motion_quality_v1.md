# C4 报告：`rgb2gray` + `motion_quality` v1 —— C 仿真 + C 综合

> 项目：知倦 / VigiLens（AMD 3.3 自主选题初级组）—— C 线（FPGA/HLS）
> 契约：`docs/interface.md` 第 3.3 / 3.4 / 4 节
> 记录人：C 线（经 AI 协助整理；**所有数字均为真实运行输出**，未估算、未代填）
> 日期：2026-09-10

---

## 1. 结论

| 验收项 | `rgb2gray` **v2** | `motion_quality` **v2** |
|---|---|---|
| C 仿真结果 | ✅ **PASS** | ✅ **PASS** |
| 第 1 层（内嵌边界用例） | ✅ **8 / 8** | ✅ **6 / 6** |
| 第 2 层（跨语言黄金参考） | ✅ **10 / 10**，不一致像素 **0** | ✅ **9 / 9**，流头错误 **0** |
| CSim | ✅ `0 errors` | ✅ `0 errors` |
| 流水线 II | ✅ **Final II = 1**（Depth 6） | ✅ **Final II = 1**（Depth 3） |
| 时序 | ✅ Estimated **7.28 ns** < 10 ns → **137.46 MHz** | ✅ Estimated **7.140 ns** < 10 ns → **140.05 MHz** |
| 综合 | ✅ 无 ERROR | ✅ 无 ERROR |
| 资源 | LUT 1410 / FF 918 / **BRAM 0** / DSP 5 | LUT 1501 / FF 1158 / **BRAM 64** / DSP 1 |
| 工作尺寸 | 640×480 RGB → **384×288 灰度** | **384×288** 灰度（片内缓存 110,592 B） |

> ✅ **BRAM 问题已闭环**：初版 `motion_quality` 在 640×480 上工作，占 **256 个 BRAM18 = 91%**（上板必炸）；
> 现已把它降到 384×288，占用 **64 个 = 23%**。根因（片内数组按 **2 的幂地址空间**分配）
> 用三组对照实验坐实，方案 A′ 已实现并验证 —— 见第 5 节。

**C4 任务达成。** 三个 IP 合计 BRAM 64/280 = 23%，为 M3 的 DMA 与互连留出了充足余量。

---

## 2. 本次新增/改动

| 文件 | 说明 |
|---|---|
| `fpga/src/rgb2gray.cpp` | **新增 IP**：RGB888 → 8bit 灰度流式转换 |
| `fpga/src/motion_quality.cpp` | **新增 IP**：帧差运动量 + 运动像素比例（片内上一帧 BRAM 缓存） |
| `fpga/sim/tb_rgb2gray.cpp` | 两层测试台（系数边界 + 逐像素黄金比对） |
| `fpga/sim/tb_motion_quality.cpp` | 两层测试台（阈值边界 + 按帧序黄金比对） |
| `fpga/sim/gen_motion_vectors.py` | **新增**：`data_motion/` 的测试向量与 Python 黄金参考生成器 |
| `fpga/run_hls.tcl` | **改造为多 IP 通用**：用环境变量 `HLS_IP` 选择（默认 `roi_statistic`） |

**为什么新增 `rgb2gray`**：原契约里 `motion_quality` 要吃 `gray_in`，但设计中**没有任何环节产生灰度**；
而 `docs/00` §3.4 明确要求"PL 端完成 RGB/YUV 转换、缩放灰度化"。若改由 PS 转灰度再 DMA，
PL 就只剩帧差，会被看作转发器（《01》4.2 点名的致命短板）。故在 PL 内补上这一环。

---

## 3. 运行命令

```bat
call D:\Xilinx\2026.1\Vitis\settings64.bat
cd /d D:\Desktop\AMD\fpga

python sim\gen_motion_vectors.py          :: 生成 data_motion/

set HLS_IP=rgb2gray
vitis-run --mode hls --tcl run_hls.tcl

set HLS_IP=motion_quality
vitis-run --mode hls --tcl run_hls.tcl
```

> `set "HLS_IP=xxx"` 加引号可避免 cmd 把尾随空格写进变量值（否则 TCL 的精确匹配会失败）。

`run_hls.tcl` 默认仍是 `roi_statistic`，因此 C3 的复现步骤与归档结论**不受本次改动影响**。

---

## 4. 测试向量与黄金参考（`fpga/sim/data_motion/`）

10 帧 640×480，刻意设计成能覆盖关键边界：

| 帧 | 内容 | 用途 |
|---|---|---|
| 0 | 随机 RGB | rgb2gray 通用覆盖；motion 的底帧（其输出**不参与比对**） |
| 1, 2 | 全零 ×2 | 互为**相同帧** → 运动量应为 0 |
| 3, 4 | 全满 ×2 | 互为**相同帧** → 运动量应为 0 |
| 5 | 仅正中像素为白 | 单点变化 |
| 6 | 全零 | 相对帧 5 是单点变化 → `diff_total` 应恰为 255 |
| 7, 8, 9 | 纯红 / 纯绿 / 纯蓝 | **直接检验三个灰度系数** |

Python 黄金参考（`gen_motion_vectors.py`）的关键输出，均可人工核对：

```
灰度公式自检:  gray(255,0,0)=77  gray(0,255,0)=149  gray(0,0,255)=29
               gray(255,255,255)=255  gray(0,0,0)=0
运动量:  frame2 vs 1 (相同帧)      diff_total=0
         frame3 vs 2 (全零→全满)   diff_total=78336000   (=307200x255)
         frame6 vs 5 (单点变化)    diff_total=255  motion_pixels=1
[numpy 对拍] PASS —— 灰度与运动量均与 numpy 独立实现一致（9 帧对）
```

**灰色公式（已冻结，A 线必须用同一式）**：`Y = (77*R + 150*G + 29*B + 128) >> 8`
系数是 BT.601 的 0.299/0.587/0.114 按 ×256 四舍五入，且 77+150+29 = 256 恰好 →
纯白 255、纯黑 0、**无需裁剪**、16 bit 内最大 65,408 不溢出。

---

## 5. ⚠️ 关键发现：`motion_quality` 占用 91% BRAM

### 5.1 实测数据

`csynth.rpt` 的存储实现表：

```
| prev_buf_U | ram_2p | ... | 256 | ... | pragma | prev_buf | bram | 1 | 8, 307200, 1 |
```

- 缓存规模：**307200 深 × 8 bit**（= 307,200 B）
- 实现：`RAM_2P` + `BRAM`（`BIND_STORAGE` 生效，日志确认 `..._prev_buf_RAM_2P_BRAM_1R1W using block RAMs`）
- **占用 256 个 BRAM18K = 器件的 91%**（PYNQ-Z2 / xc7z020 共 280 个）

### 5.2 根因（已用对照实验坐实）

**结论：BRAM 按「2 的幂地址空间」分配，而不是按真实深度。**

对照实验：只改 `MOTION_MAX_W/H` 与配套数据尺寸，其余不动，各重跑一次完整 csim+csynth
（`320×240`、`512×512` 各一次，实验后已还原为 `640×480`）：

| 尺寸 | 缓存字节 | 地址位宽 | 实测 BRAM18 | 按 2 的幂地址空间<br>`2^⌈log2(depth)⌉ ÷ 2048` | 按真实数据量<br>`depth ÷ 2048` |
|---|---|---|---|---|---|
| 320×240 | 76,800 | 17 | **64** | **64** ✅ | 38 ✗ |
| 512×512 | 262,144 | 18 | **128** | **128** ✅ | 128 ✅ |
| 640×480 | 307,200 | 19 | **256** | **256** ✅ | 150 ✗ |

三个数据点**全部**符合"2 的幂地址空间"这一解释，而"按真实数据量"被其中两点直接否证。

即：`depth = 307200` 落在 2¹⁸(262144) ~ 2¹⁹(524288) 之间 → 工具按 **2¹⁹ = 524288** 的地址空间实现
→ 524288 ÷ 2048 = **256** 个 BRAM18。比"按数据量"多出约 **1.7 倍**。

> 副作用提醒：这条规律意味着**缓存成本是台阶式的** —— 只要跨过一个 2 的幂就翻倍。
> 反过来说，"像素数 ≤ 131072" 的任意尺寸都只需 **64** 个 BRAM18，
> 所以**在同一个台阶内可以挑更大的分辨率而不用多花 BRAM**（见 5.4）。

### 5.3 为什么必须解决

M3 的 Block Design 里除本 IP 外还要放 AXI DMA、AXI Interconnect、以及 `roi_statistic` / `rgb2gray` / `fir_filter`。
剩下只有 24 个 BRAM18，**极可能放不下**，会直接卡死上板。

### 5.4 方案决策：**已选 A′（384×288）并实现**

| 方案 | 运动检测工作分辨率 | 缓存字节 | 地址位宽 | **BRAM18** | 占比 | 代价 |
|---|---|---|---|---|---|---|
| A. 降分辨率 | 320×240 | 76,800 | 17 | **64（已实测）** | 23% | 需在 `rgb2gray` 后加缩放 |
| ✅ **A′. 降分辨率（同价更清晰）· 已采用** | **384×288**（=110,592） | 110,592 | 17 | **64（已实测）** | **23%** | 同上；**同样 64 个 BRAM 却保留更高分辨率**（没跨过 2¹⁷） |
| 中间档 | 512×384（=196,608） | 196,608 | 18 | 128（推算） | 46% | 只降 0.8 倍，BRAM 仍近半 |
| B. 双流方案 | 640×480 | 0 | — | 0 | 0% | IP 同收"当前帧+上一帧"两条流，PL 不存帧；DMA 流量翻倍，"片内缓存"展示点丢失 |
| C. 接受现状（初版） | 640×480 | 307,200 | 19 | **256（已实测）** | 91% | 上板必炸 |

**实施结果（已实测验证）**：

| | 初版 | **A′ 实施后** |
|---|---|---|
| `motion_quality` 工作尺寸 | 640×480 | **384×288** |
| `motion_quality` BRAM18 | 256（**91%**） | **64（23%）** |
| `motion_quality` csim | 6/6 + 9/9 | **6/6 + 9/9**（仍全过） |
| `motion_quality` Fmax | 140.05 MHz | **140.05 MHz**（未退化） |
| `rgb2gray` | v1：仅灰度化 | **v2：灰度化 + 3/5 抽取**（csim 8/8 + 10/10，Fmax 137.46 MHz，BRAM 0/DSP 5） |
| 留给 M3 的 BRAM18 | 24 | **216** |

> 逻辑资源几乎不受影响（各分辨率下 LUT 均约 1500）。
> **新增的已知取舍**：3/5 是**点采样**（保留原始灰度值），会保留混叠，运动检测可能偏"敏感"。
> 若接真实视频后发现运动量偏噪，改块均值即可（黄金参考同步改），或退回 320×240 的 2×2 均值。
> 已作为"待观察"写进契约 §5 第 7 项。

---

## 6. 逐条警告判读

| 警告 | 判读 |
|---|---|
| `[RTGEN 206-101] Design contains AXI ports. Reset is fixed to synchronous and active low.` | 有 AXI 端口时的固定行为，正常 |
| `[RTGEN 206-101] Register 'fid' is power-on initialization.` | `static frame_id` 是**上电**初始化而非复位归零。本设计不依赖复位归零，且测试台只要求它单调递增，故可接受 |
| `[RTMG 210-278] Implementing memory 'prev_buf...' using block RAMs` | 期望行为（BIND_STORAGE 生效） |
| `[HLS 207-4637] use of undeclared identifier 'prev_buf'`（**已修复**） | 见第 7 节 |

---

## 7. 本次踩坑（已修复，已沉淀进 skill）

### 7.1 `BIND_STORAGE` pragma 必须写在变量声明**之后**

第一次跑 `motion_quality` 时 **csim 通过、csynth 失败**：

```
ERROR: [HLS 207-4637] use of undeclared identifier 'prev_buf' (src/motion_quality.cpp:67:35)
```

原因：`#pragma HLS BIND_STORAGE variable=prev_buf ...` 被写在 `prev_buf` 声明**之前**，
而 HLS 按顺序解析 pragma。把 pragma 移到声明之后即通过。

> **教训**：**csim 不检查这条 pragma**，所以这类错误只在 csynth 暴露 ——
> "csim 全绿"绝不等于"综合能过"。已写入 `skill/fpga_hls_c_line.md`。

### 7.2 灰度口径：与"浮点四舍五入"差 1 LSB（**这不是 bug，但必须写进契约**）

`0.299 × 255 = 76.245 → 76`，而本设计冻结的整数式给 **77**。
若 A 线直接用 `cv2.cvtColor` 做黄金参考，会与硬件产生**系统性 ±1 偏差**。
故契约把一个 3 行就能实现的整数式冻结为唯一口径，A 线若要用 OpenCV 必须先对拍。

---

## 8. 三个 IP 汇总（截至本次，全部真实数据）

| IP | 工作尺寸 | csim | II | Fmax | LUT | FF | BRAM18 | DSP |
|---|---|---|---|---|---|---|---|---|
| `roi_statistic` | 640×480 RGB | 28/28 + 45/45 | 1 | 138.99 MHz | 1267 | 723 | 0 | 1 |
| `rgb2gray` **v2** | 640×480 → 384×288 | 8/8 + 10/10 | 1 | 137.46 MHz | 1410 | 918 | 0 | 5 |
| `motion_quality` **v2** | 384×288 灰度 | 6/6 + 9/9 | 1 | 140.05 MHz | 1501 | 1158 | **64** | 1 |
| **合计** | | | | | **4178** | **2799** | **64** | **7** |

> LUT 合计 4178 / 53200 = **7.9%**；FF 2799 / 106400 = **2.6%**；DSP 7 / 220 = **3.2%**；
> **BRAM 64 / 280 = 22.9%**。
> 三个 IP 全部 II=1、全部 Fmax > 100 MHz（目标 10 ns），**为 M3 的 AXI DMA 与互连留出了 216 个 BRAM18**。
>
> 对比：如果不做 3/5 缩放，`motion_quality` 会占 256 个 BRAM18（91%），M3 只剩 24 个 —— 基本必炸。

---

## 9. 待办

- [x] ~~决策第 5.4 节的 BRAM 方案~~ → ✅ **已选 A′（384×288）并实现验证**，BRAM 256 → 64
- [ ] `fir_filter`（C5）
- [ ] 四个 IP 各跑一次 `hls_exec = 2` 的 cosim（csim 查不出流深度死锁）
- [ ] `motion_thresh` 默认值 16 待 A 线用其口径复核后冻结
- [ ] A 线按契约 **4.4 节**用 NumPy 对 `data_motion/gray.bin` 复算对拍（灰度 + 缩放两个口径）
- [ ] **待观察**：3/5 点采样会保留混叠，接真实视频后确认运动量是否偏噪；若偏噪则改块均值或退回 320×240 的 2×2 均值

---

*本文件由 C 线维护。所有数字均为真实运行输出的抄录；如需更新，请附新的运行日志。*
